const BASE_URL = 'http://10.58.52.102:8000';

export const API = {
  MAIN: `${BASE_URL}/main`,
  KAKAO_LOGIN: `${BASE_URL}/auth/kakao/callback`,
};

// 사용하는 컴포넌트
// import { API } from "config";

// fetch(`${API.MYPAGE}/5`).then(...).then(...);
